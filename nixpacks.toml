# nixpacks.toml
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "streamlit run scripts/06_dashboard.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"